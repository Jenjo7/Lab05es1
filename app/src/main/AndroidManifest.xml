<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.unibo.studio.savini.gianni.lab05es1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".MyReciver">
            <intent-filter>
                <action android:name="it.unibo.studio.savini.gianni.lab05es1.ACTION"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".MyReciver2">
            <intent-filter>
                <action android:name="it.unibo.studio.savini.gianni.lab05es1.ACTION"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".MyReciver3">
            <intent-filter>
                <action android:name="it.unibo.studio.savini.gianni.lab05es1.ACTION"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>